---
description: Skill buckets for developing ahar-visualizer itself.
---

- `dev-preview/` — how to check a change in a real VS Code window safely, without risking whatever session is doing the checking. See `dev-preview/SKILL.md`.
